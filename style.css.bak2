/* ══════════════════════════════════════════════
   🚀 AutoPilot Hub — SUPER PREMIUM STYLESHEET
   Freshtiq Innovations OPC Pvt Ltd
   ══════════════════════════════════════════════ */

/* ── CSS Variables ── */
:root {
  --primary: #0a0a1a;
  --secondary: #111128;
  --accent: #00e5ff;
  --accent2: #7c3aed;
  --accent3: #ff6b35;
  --gold: #ffd700;
  --pink: #ff2d55;
  --text: #e8e8f0;
  --text-muted: #8888aa;
  --card: rgba(255,255,255,0.03);
  --card-border: rgba(255,255,255,0.06);
  --glass: rgba(255,255,255,0.05);
  --shadow: 0 8px 40px rgba(0,0,0,0.4);
  --shadow-glow: 0 0 40px rgba(0,229,255,0.1);
  --radius: 20px;
  --radius-sm: 12px;
}

/* ── Reset & Base ── */
*, *::before, *::after { margin: 0; padding: 0; box-sizing: border-box; }
html { scroll-behavior: smooth; }

body {
  font-family: 'Segoe UI', system-ui, -apple-system, sans-serif;
  background: var(--primary);
  color: var(--text);
  overflow-x: hidden;
  line-height: 1.7;
}

/* ── Animated Galaxy Background ── */
#galaxy-bg {
  position: fixed;
  top: 0; left: 0; width: 100%; height: 100%;
  z-index: -1;
  overflow: hidden;
}
#galaxy-bg canvas { position: absolute; top: 0; left: 0; }

/* ── Scrollbar ── */
::-webkit-scrollbar { width: 8px; }
::-webkit-scrollbar-track { background: var(--primary); }
::-webkit-scrollbar-thumb { background: linear-gradient(135deg, var(--accent), var(--accent2)); border-radius: 10px; }

/* ── Top Announcement Bar ── */
.top-bar {
  background: linear-gradient(90deg, var(--accent2), var(--accent), var(--accent2));
  background-size: 200% 100%;
  animation: shimmer 3s ease-in-out infinite;
  color: #fff;
  text-align: center;
  padding: 10px;
  font-size: 0.85rem;
  font-weight: 600;
  position: relative;
  z-index: 100;
  letter-spacing: 0.5px;
}
@keyframes shimmer {
  0%, 100% { background-position: 0% 50%; }
  50% { background-position: 100% 50%; }
}
.top-bar a { color: #0a0a1a; text-decoration: none; font-weight: 700; }
.top-bar a:hover { text-decoration: underline; }

/* ── Navigation ── */
nav {
  position: sticky; top: 0;
  background: rgba(10,10,26,0.85);
  backdrop-filter: blur(30px) saturate(1.5);
  border-bottom: 1px solid rgba(255,255,255,0.05);
  z-index: 99;
}
.nav-container {
  max-width: 1200px; margin: 0 auto;
  display: flex; justify-content: space-between; align-items: center;
  height: 72px; padding: 0 20px;
}
.nav-logo {
  display: flex; align-items: center; gap: 12px;
  font-size: 1.3rem; font-weight: 800; color: #fff;
  text-decoration: none;
}
.logo-icon {
  width: 42px; height: 42px;
  background: linear-gradient(135deg, var(--accent), var(--accent2));
  border-radius: 12px;
  display: flex; align-items: center; justify-content: center;
  font-size: 1.3rem; box-shadow: 0 0 20px rgba(0,229,255,0.3);
}
.nav-links { display: flex; gap: 8px; list-style: none; align-items: center; }
.nav-links a {
  padding: 8px 16px;
  color: var(--text-muted); text-decoration: none;
  font-size: 0.9rem; font-weight: 500;
  border-radius: 10px; transition: all 0.3s;
}
.nav-links a:hover { color: #fff; background: rgba(255,255,255,0.06); }
.nav-whatsapp {
  background: #25d366 !important;
  color: #fff !important;
  padding: 10px 24px !important;
  border-radius: 50px !important;
  font-weight: 600 !important;
  display: flex; align-items: center; gap: 8px;
  transition: transform 0.3s !important;
}
.nav-whatsapp:hover { transform: scale(1.05) !important; background: #22c35e !important; }

.lang-switcher { display: flex; gap: 4px; align-items: center; margin: 0 8px; }
.lang-btn {
  background: rgba(255,255,255,0.06);
  border: 1px solid rgba(255,255,255,0.1);
  color: var(--text-muted);
  padding: 5px 10px; border-radius: 8px;
  font-size: 0.75rem; font-weight: 700; cursor: pointer;
  transition: all 0.3s;
}
.lang-btn:hover { background: rgba(255,255,255,0.12); color: #fff; }
.lang-btn.active { background: linear-gradient(135deg, var(--accent), var(--accent2)); border-color: transparent; color: #fff; box-shadow: 0 0 15px rgba(0,229,255,0.2); }

.mobile-toggle { display: none; background: none; border: none; color: #fff; font-size: 1.5rem; cursor: pointer; }

/* ── Hero ── */
.hero {
  padding: 120px 20px 100px;
  text-align: center;
  position: relative;
  overflow: hidden;
}
.hero::before {
  content: '';
  position: absolute; top: 50%; left: 50%;
  width: 900px; height: 900px;
  background: radial-gradient(circle, rgba(0,229,255,0.06) 0%, transparent 60%);
  transform: translate(-50%, -50%);
  animation: heroPulse 6s ease-in-out infinite;
}
.hero::after {
  content: '';
  position: absolute; top: 30%; right: 5%;
  width: 400px; height: 400px;
  background: radial-gradient(circle, rgba(124,58,237,0.05) 0%, transparent 50%);
  animation: heroPulse 8s ease-in-out infinite reverse;
}
@keyframes heroPulse {
  0%, 100% { transform: translate(-50%, -50%) scale(1); }
  50% { transform: translate(-50%, -50%) scale(1.3); }
}
.hero-content { position: relative; z-index: 1; max-width: 850px; margin: 0 auto; }
.hero-badge {
  display: inline-flex; align-items: center; gap: 8px;
  background: rgba(0,229,255,0.1);
  border: 1px solid rgba(0,229,255,0.2);
  padding: 8px 20px; border-radius: 50px;
  font-size: 0.8rem; font-weight: 600; color: var(--accent);
  margin-bottom: 30px;
  backdrop-filter: blur(10px);
}
.hero h1 {
  font-size: 4rem;
  font-weight: 900;
  line-height: 1.05;
  margin-bottom: 25px;
  background: linear-gradient(135deg, #fff 20%, var(--accent) 50%, var(--accent2) 80%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  animation: textShimmer 4s ease-in-out infinite;
  background-size: 200% 200%;
}
@keyframes textShimmer {
  0%, 100% { background-position: 0% 50%; }
  50% { background-position: 100% 50%; }
}
.hero p { font-size: 1.15rem; color: rgba(255,255,255,0.55); max-width: 650px; margin: 0 auto 40px; line-height: 1.9; }
.hero-buttons { display: flex; gap: 15px; justify-content: center; flex-wrap: wrap; }

.btn-primary {
  display: inline-flex; align-items: center; gap: 10px;
  background: linear-gradient(135deg, var(--accent), var(--accent2));
  color: #fff !important;
  padding: 16px 40px; border-radius: 50px;
  font-size: 1rem; font-weight: 700; text-decoration: none;
  transition: all 0.4s;
  box-shadow: 0 4px 30px rgba(0,229,255,0.3);
  position: relative; overflow: hidden;
}
.btn-primary::before {
  content: ''; position: absolute; top: 0; left: -100%;
  width: 100%; height: 100%;
  background: linear-gradient(90deg, transparent, rgba(255,255,255,0.2), transparent);
  transition: left 0.6s;
}
.btn-primary:hover::before { left: 100%; }
.btn-primary:hover { transform: translateY(-3px); box-shadow: 0 8px 40px rgba(0,229,255,0.5); }

.btn-secondary {
  display: inline-flex; align-items: center; gap: 10px;
  background: rgba(255,255,255,0.06);
  border: 1px solid rgba(255,255,255,0.12);
  color: #fff !important;
  padding: 16px 40px; border-radius: 50px;
  font-size: 1rem; font-weight: 600; text-decoration: none;
  transition: all 0.3s;
}
.btn-secondary:hover { background: rgba(255,255,255,0.12); transform: translateY(-3px); }

/* ── Hero Social Proof ── */
.hero-proof {
  display: flex; justify-content: center; gap: 40px;
  margin-top: 50px; flex-wrap: wrap;
}
.hero-proof-item { text-align: center; }
.hero-proof-item .num {
  font-size: 2.5rem; font-weight: 900;
  background: linear-gradient(135deg, var(--accent), var(--accent2));
  -webkit-background-clip: text; -webkit-text-fill-color: transparent;
  background-clip: text;
}
.hero-proof-item .label { color: var(--text-muted); font-size: 0.85rem; margin-top: 4px; }

/* ── Section ── */
.section { padding: 100px 20px; }
.container { max-width: 1200px; margin: 0 auto; }
.section-tag {
  display: inline-flex; align-items: center; gap: 8px;
  color: var(--accent);
  font-size: 0.8rem; font-weight: 700;
  text-transform: uppercase; letter-spacing: 2px;
  margin-bottom: 12px;
  background: rgba(0,229,255,0.08);
  padding: 6px 16px; border-radius: 50px;
  border: 1px solid rgba(0,229,255,0.15);
}
.section h2 {
  font-size: 2.8rem; font-weight: 900;
  margin-bottom: 60px; text-align: center; line-height: 1.2;
}
.section h2 span {
  background: linear-gradient(135deg, var(--accent), var(--accent2));
  -webkit-background-clip: text; -webkit-text-fill-color: transparent;
  background-clip: text;
}

/* ── Services ── */
.services-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(340px, 1fr));
  gap: 24px;
}
.service-card {
  background: var(--card);
  backdrop-filter: blur(10px);
  border: 1px solid var(--card-border);
  padding: 40px 32px;
  border-radius: var(--radius);
  transition: all 0.5s;
  position: relative;
  overflow: hidden;
}
.service-card::before {
  content: ''; position: absolute; top: 0; left: 0;
  width: 100%; height: 3px;
  background: linear-gradient(90deg, var(--accent), var(--accent2));
  transform: scaleX(0); transform-origin: left;
  transition: transform 0.5s;
}
.service-card::after {
  content: ''; position: absolute; top: 0; right: 0;
  width: 100%; height: 100%;
  background: radial-gradient(600px circle at var(--mouse-x, 50%) var(--mouse-y, 50%), rgba(0,229,255,0.04), transparent 60%);
  opacity: 0; transition: opacity 0.5s;
  pointer-events: none;
}
.service-card:hover { transform: translateY(-8px); border-color: rgba(0,229,255,0.15); box-shadow: var(--shadow); }
.service-card:hover::before { transform: scaleX(1); }
.service-card:hover::after { opacity: 1; }
.service-icon { font-size: 2.8rem; margin-bottom: 20px; display: block; }
.service-card h3 { font-size: 1.3rem; margin-bottom: 12px; color: #fff; }
.service-card p { color: var(--text-muted); font-size: 0.9rem; margin-bottom: 20px; line-height: 1.8; }
.service-price {
  font-size: 1.5rem; font-weight: 800;
  background: linear-gradient(135deg, var(--accent), var(--accent2));
  -webkit-background-clip: text; -webkit-text-fill-color: transparent;
  background-clip: text;
}
.service-price small { font-size: 0.8rem; font-weight: 400; color: var(--text-muted); -webkit-text-fill-color: var(--text-muted); }

/* ── Why Us ── */
.why-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
  gap: 24px;
}
.why-card {
  text-align: center;
  padding: 40px 25px;
  background: var(--card);
  backdrop-filter: blur(10px);
  border: 1px solid var(--card-border);
  border-radius: var(--radius);
  transition: all 0.5s;
}
.why-card:hover { transform: translateY(-8px); border-color: rgba(0,229,255,0.15); box-shadow: var(--shadow); }
.why-icon {
  width: 64px; height: 64px; margin: 0 auto 20px;
  background: linear-gradient(135deg, rgba(0,229,255,0.1), rgba(124,58,237,0.1));
  border-radius: 18px;
  display: flex; align-items: center; justify-content: center;
  font-size: 1.6rem;
  transition: transform 0.5s;
}
.why-card:hover .why-icon { transform: scale(1.1) rotate(5deg); }
.why-card h3 { color: #fff; margin-bottom: 10px; font-size: 1.1rem; }
.why-card p { color: var(--text-muted); font-size: 0.9rem; line-height: 1.7; }

/* ── Pricing ── */
.pricing-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(330px, 1fr));
  gap: 24px;
}
.pricing-card {
  background: var(--card);
  backdrop-filter: blur(10px);
  border: 1px solid var(--card-border);
  padding: 45px 30px 35px;
  border-radius: var(--radius);
  text-align: center;
  transition: all 0.5s;
  position: relative;
  overflow: hidden;
}
.pricing-card.featured {
  border-color: var(--accent);
  transform: scale(1.03);
  box-shadow: var(--shadow-glow), var(--shadow);
}
.pricing-card.featured::before {
  content: '🔥 BESTSELLER';
  position: absolute; top: 12px; left: 50%;
  transform: translateX(-50%);
  background: linear-gradient(135deg, var(--accent), var(--accent2));
  color: #fff;
  padding: 4px 18px; border-radius: 50px;
  font-size: 0.7rem; font-weight: 700; letter-spacing: 1px;
}
.pricing-card:hover { transform: translateY(-5px); border-color: rgba(0,229,255,0.15); box-shadow: var(--shadow); }
.pricing-card.featured:hover { transform: scale(1.03) translateY(-5px); }
.plan-name { font-size: 1.4rem; font-weight: 700; color: #fff; margin-bottom: 5px; }
.plan-desc { color: var(--text-muted); font-size: 0.85rem; margin-bottom: 25px; }
.plan-price {
  font-size: 3.2rem; font-weight: 900;
  background: linear-gradient(135deg, var(--accent), var(--accent2));
  -webkit-background-clip: text; -webkit-text-fill-color: transparent;
  background-clip: text;
}
.plan-price span { font-size: 1rem; font-weight: 400; color: var(--text-muted); -webkit-text-fill-color: var(--text-muted); }
.pricing-features { list-style: none; padding: 0; margin: 25px 0; text-align: left; }
.pricing-features li {
  padding: 10px 0; border-bottom: 1px solid rgba(255,255,255,0.04);
  color: rgba(255,255,255,0.65); font-size: 0.9rem;
}
.pricing-features li::before { content: '✅ '; }
.btn-order {
  display: inline-block;
  background: rgba(255,255,255,0.06);
  border: 1px solid rgba(255,255,255,0.12);
  color: #fff;
  padding: 14px 35px; border-radius: 50px;
  text-decoration: none; font-weight: 600;
  transition: all 0.3s; width: 100%;
}
.btn-order:hover { background: linear-gradient(135deg, var(--accent), var(--accent2)); border-color: transparent; transform: translateY(-2px); }

/* ── Reviews / Testimonials ── */
.testimonials-section {
  background: linear-gradient(180deg, transparent, rgba(0,229,255,0.02), transparent);
}
.reviews-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
  gap: 20px;
}
.review-card {
  background: var(--card);
  backdrop-filter: blur(10px);
  border: 1px solid var(--card-border);
  padding: 28px;
  border-radius: var(--radius);
  transition: all 0.4s;
  position: relative;
}
.review-card:hover { transform: translateY(-5px); border-color: rgba(0,229,255,0.12); box-shadow: var(--shadow); }
.review-header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 12px; }
.review-stars { color: var(--gold); font-size: 0.95rem; letter-spacing: 2px; }
.review-date { color: var(--text-muted); font-size: 0.75rem; }
.review-text { font-style: italic; color: rgba(255,255,255,0.7); font-size: 0.9rem; line-height: 1.7; margin-bottom: 12px; }
.review-author { display: flex; align-items: center; gap: 10px; }
.review-avatar {
  width: 36px; height: 36px;
  border-radius: 50%;
  background: linear-gradient(135deg, var(--accent), var(--accent2));
  display: flex; align-items: center; justify-content: center;
  font-size: 0.8rem; font-weight: 700; color: #fff;
}
.review-name { font-weight: 600; font-size: 0.85rem; color: #fff; }
.review-role { color: var(--text-muted); font-size: 0.75rem; }

/* ── Trust Badges ── */
.trust-bar {
  background: rgba(255,255,255,0.02);
  border-top: 1px solid rgba(255,255,255,0.04);
  border-bottom: 1px solid rgba(255,255,255,0.04);
  padding: 30px 20px;
}
.trust-grid {
  display: flex; justify-content: center; gap: 40px; flex-wrap: wrap; align-items: center;
}
.trust-item {
  display: flex; align-items: center; gap: 8px;
  color: var(--text-muted); font-size: 0.85rem;
}
.trust-item .icon { font-size: 1.5rem; }

/* ── Live Demo / CTA Banner ── */
.demo-banner {
  background: linear-gradient(135deg, rgba(0,229,255,0.05), rgba(124,58,237,0.05));
  border: 1px solid rgba(0,229,255,0.1);
  border-radius: 24px;
  padding: 60px 40px;
  text-align: center;
  position: relative;
  overflow: hidden;
}
.demo-banner::before {
  content: '';
  position: absolute; top: -50%; left: -50%;
  width: 200%; height: 200%;
  background: radial-gradient(circle at 30% 50%, rgba(0,229,255,0.04) 0%, transparent 50%);
  animation: demoFloat 10s ease-in-out infinite;
}
@keyframes demoFloat {
  0%, 100% { transform: translate(0, 0); }
  50% { transform: translate(20px, -20px); }
}
.demo-banner h3 { font-size: 2rem; font-weight: 800; color: #fff; margin-bottom: 15px; position: relative; z-index: 1; }
.demo-banner p { color: var(--text-muted); max-width: 600px; margin: 0 auto 30px; position: relative; z-index: 1; }
.demo-steps {
  display: flex; justify-content: center; gap: 30px; flex-wrap: wrap;
  margin-bottom: 30px; position: relative; z-index: 1;
}
.demo-step {
  display: flex; align-items: center; gap: 10px;
  background: rgba(255,255,255,0.04);
  padding: 12px 20px; border-radius: 12px;
  border: 1px solid rgba(255,255,255,0.06);
}
.demo-step .num {
  width: 30px; height: 30px;
  background: linear-gradient(135deg, var(--accent), var(--accent2));
  border-radius: 50%;
  display: flex; align-items: center; justify-content: center;
  font-size: 0.8rem; font-weight: 700; color: #fff;
}
.demo-step .text { color: var(--text-muted); font-size: 0.9rem; }
.demo-step .text strong { color: #fff; }

/* ── Company Section ── */
.company-section {
  background: linear-gradient(180deg, transparent, rgba(124,58,237,0.03));
}
.company-grid {
  display: grid;
  grid-template-columns: 1.2fr 0.8fr;
  gap: 40px;
  align-items: center;
}
.company-info h3 { font-size: 1.8rem; font-weight: 800; color: #fff; margin-bottom: 20px; }
.company-info p { color: var(--text-muted); margin-bottom: 15px; font-size: 1rem; line-height: 1.8; }
.company-details { list-style: none; }
.company-details li {
  padding: 12px 0; border-bottom: 1px solid rgba(255,255,255,0.04);
  color: rgba(255,255,255,0.7);
}
.company-details li strong { color: var(--accent); }

.company-badge {
  background: var(--card);
  backdrop-filter: blur(10px);
  border: 1px solid var(--card-border);
  padding: 50px 35px;
  border-radius: 24px;
  text-align: center;
}
.company-badge .reg-icon {
  width: 70px; height: 70px; margin: 0 auto 20px;
  background: linear-gradient(135deg, rgba(0,229,255,0.1), rgba(124,58,237,0.1));
  border-radius: 20px;
  display: flex; align-items: center; justify-content: center;
  font-size: 2rem;
}
.company-badge h4 { font-size: 1.6rem; margin-bottom: 5px; }
.company-badge h4 span { background: linear-gradient(135deg, var(--accent), var(--accent2)); -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text; }
.company-badge .sub { color: var(--text-muted); font-size: 0.9rem; margin-bottom: 20px; }
.company-badge .tagline { font-style: italic; color: var(--accent); font-size: 1.1rem; }

/* ── Trust Badges Strip ── */
.badges-strip {
  display: flex; justify-content: center; gap: 20px; flex-wrap: wrap;
  margin-top: 30px;
}
.badge-item {
  background: var(--card);
  border: 1px solid var(--card-border);
  padding: 12px 20px; border-radius: 12px;
  display: flex; align-items: center; gap: 8px;
  color: var(--text-muted); font-size: 0.8rem;
}

/* ── Contact ── */
.contact-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 40px;
}
.contact-info h3 { font-size: 1.5rem; margin-bottom: 20px; color: #fff; }
.contact-info p { margin-bottom: 12px; color: var(--text-muted); font-size: 1rem; }
.contact-info a { color: var(--accent); text-decoration: none; font-weight: 600; transition: color 0.3s; }
.contact-info a:hover { color: var(--accent2); }
.contact-tagline { color: var(--text-muted) !important; font-style: italic; margin-top: 20px; font-size: 1rem !important; }
.contact-cta { display: flex; flex-direction: column; align-items: center; justify-content: center; gap: 20px; }

/* ── WhatsApp Float ── */
.whatsapp-float {
  position: fixed; bottom: 25px; right: 25px; z-index: 1000;
}
.whatsapp-float a {
  display: flex; align-items: center; justify-content: center;
  width: 60px; height: 60px;
  background: #25d366;
  border-radius: 50%;
  font-size: 1.8rem; color: #fff; text-decoration: none;
  box-shadow: 0 4px 25px rgba(37,211,102,0.4);
  animation: pulseWA 2s infinite;
  transition: all 0.3s;
}
.whatsapp-float a:hover { transform: scale(1.1); }
@keyframes pulseWA {
  0% { box-shadow: 0 4px 25px rgba(37,211,102,0.4); }
  50% { box-shadow: 0 4px 40px rgba(37,211,102,0.7); }
  100% { box-shadow: 0 4px 25px rgba(37,211,102,0.4); }
}
.whatsapp-tooltip {
  position: absolute; right: 70px; top: 50%;
  transform: translateY(-50%);
  background: rgba(0,0,0,0.9);
  color: #fff; padding: 8px 15px; border-radius: 8px;
  font-size: 0.8rem; white-space: nowrap;
  opacity: 0; transition: opacity 0.3s;
  pointer-events: none; backdrop-filter: blur(10px);
}
.whatsapp-float:hover .whatsapp-tooltip { opacity: 1; }

/* ── Footer ── */
footer {
  background: rgba(0,0,0,0.4);
  backdrop-filter: blur(10px);
  border-top: 1px solid rgba(255,255,255,0.04);
  text-align: center;
  padding: 50px 20px 30px;
}
.footer-line {
  width: 60px; height: 3px;
  background: linear-gradient(90deg, var(--accent), var(--accent2));
  margin: 0 auto 20px; border-radius: 3px;
}
footer p { color: var(--text-muted); font-size: 0.85rem; margin-bottom: 5px; }
footer a { color: var(--accent); text-decoration: none; }
footer a:hover { text-decoration: underline; }
.footer-links { display: flex; justify-content: center; gap: 20px; flex-wrap: wrap; margin: 15px 0; }
.footer-links a { color: var(--text-muted); font-size: 0.85rem; text-decoration: none; transition: color 0.3s; }
.footer-links a:hover { color: var(--accent); }

/* ── Mobile ── */
@media (max-width: 768px) {
  .nav-links { display: none; }
  .mobile-toggle { display: block; }
  .nav-links.active {
    display: flex; flex-direction: column;
    position: absolute; top: 72px; left: 0; right: 0;
    background: rgba(10,10,26,0.98);
    backdrop-filter: blur(20px);
    padding: 20px; gap: 10px;
    border-bottom: 1px solid rgba(255,255,255,0.05);
    z-index: 100;
  }
  .hero h1 { font-size: 2.4rem; }
  .hero p { font-size: 1rem; }
  .hero-proof { gap: 20px; }
  .section h2 { font-size: 1.8rem; }
  .pricing-card.featured { transform: none; }
  .company-grid { grid-template-columns: 1fr; }
  .contact-grid { grid-template-columns: 1fr; }
  .demo-steps { flex-direction: column; align-items: center; }
  .lang-switcher { margin: 5px 0; }
}

@media (max-width: 480px) {
  .hero { padding: 80px 15px 60px; }
  .hero h1 { font-size: 1.8rem; }
  .btn-primary, .btn-secondary { padding: 14px 28px; font-size: 0.9rem; }
  .section { padding: 60px 15px; }
  .section h2 { font-size: 1.5rem; }
}


.product-tag {
  display: inline-block;
  background: rgba(0,229,255,0.08);
  border: 1px solid rgba(0,229,255,0.12);
  padding: 4px 14px;
  border-radius: 50px;
  font-size: 0.8rem;
  color: var(--accent);
  margin: 4px;
  transition: all 0.3s;
}
.product-tag:hover {
  background: rgba(0,229,255,0.15);
  border-color: var(--accent);
}
.products-tags { display: flex; flex-wrap: wrap; gap: 4px; }
